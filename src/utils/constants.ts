export class Constants {
    static IS_HEADLESS = false
    static dbPath = "./data/database.json"

    static defaultWaitMs = 1000 * 2 // 2 seconds
    static defaultDownloadWaitMs = 1000 * 10 // 10 seconds
}